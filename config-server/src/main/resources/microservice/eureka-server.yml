server:
  port: 8500

spring:
  application:
    name: Eureka Server

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    serviceUrl:
      defaultZone: http://localhost:8500/eureka